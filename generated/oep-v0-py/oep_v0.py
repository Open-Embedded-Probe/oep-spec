"""Generated by tools/oepgen.py from registry/oep-v0.yaml. Do not edit."""

from __future__ import annotations

import struct
from dataclasses import dataclass, field

PROTOCOL_VERSION = 0
PROTOCOL_REVISION = 0

CONST_CONFIRM_REQUEST_MAGIC = b'OEP?'
CONST_CONFIRM_RESULT_MAGIC = b'OEP!'

ROLE_REQUEST = 0x01
ROLE_RESULT = 0x02
ROLE_ACTIVITY_UPDATE = 0x03
ROLE_ACTIVITY_OUTCOME = 0x04
ROLE_NOTIFICATION = 0x05
ROLE_DATA = 0x06
ROLES = {'request': 0x01, 'result': 0x02, 'activity_update': 0x03, 'activity_outcome': 0x04, 'notification': 0x05, 'data': 0x06}

RESOLUTION_REJECTED = 0x00
RESOLUTION_COMPLETED = 0x01
RESOLUTION_ACCEPTED = 0x02
RESOLUTIONS = {'rejected': 0x00, 'completed': 0x01, 'accepted': 0x02}

OUTCOME_SUCCESS = 0x00
OUTCOME_FAILED = 0x01
OUTCOME_PARTIAL = 0x02
OUTCOMES = {'success': 0x00, 'failed': 0x01, 'partial': 0x02}

REJECT_UNKNOWN_FUNCTION = 0x01
REJECT_UNKNOWN_OPERATION = 0x02
REJECT_MALFORMED_PAYLOAD = 0x03
REJECT_UNAVAILABLE = 0x04
REJECT_BUSY = 0x05
REJECT_WINDOW_EXCEEDED = 0x06
REJECTS = {'unknown_function': 0x01, 'unknown_operation': 0x02, 'malformed_payload': 0x03, 'unavailable': 0x04, 'busy': 0x05, 'window_exceeded': 0x06}

TLV_CRITICAL_MASK = 0x80
TLV_CORE_CHANNEL_CANDIDATE = 0x01
TLV_CORE_MAX_CLOCK_HZ = 0x02
TLV_CORE_MAX_LENGTH = 0x03
TLV_CORE_EXCLUSIVE_GROUP = 0x04
TLV_CORE_ROLE_ASSIGNMENT = 0x90
TLV_CORE_START_TOGETHER = 0x91

DEF_CORE = (0x0000, 0x0000, 0)
DEF_CORE_FUNCTION = 0x0000
CORE_OP_CONFIRM = 0x01
CORE_OP_LIST = 0x02
CORE_OP_DESCRIBE = 0x03
CORE_OP_PLAN_APPLY = 0x04
CORE_OP_PLAN_RELEASE = 0x05
CORE_OP_STOP = 0x06
CORE_OP_PING = 0x07
DEF_PROBE_IDENTITY = (0x0000, 0x0001, 0)
PROBE_IDENTITY_OP_GET = 0x01
DEF_TARGET_CONTROL = (0x0000, 0x0010, 0)
TARGET_CONTROL_OP_STATUS = 0x01
TARGET_CONTROL_OP_RESET = 0x02
TARGET_CONTROL_OP_HALT = 0x03
TARGET_CONTROL_OP_RESUME = 0x04
TARGET_CONTROL_OP_READ_DMI = 0x10
TARGET_CONTROL_OP_READ_REGISTER = 0x11
DEF_TARGET_MEMORY = (0x0000, 0x0011, 0)
TARGET_MEMORY_OP_READ = 0x01
TARGET_MEMORY_OP_WRITE = 0x02
DEF_TARGET_FLASH = (0x0000, 0x0012, 0)
TARGET_FLASH_OP_GEOMETRY = 0x01
TARGET_FLASH_OP_ERASE_PAGE = 0x02
TARGET_FLASH_OP_PROGRAM_PAGE = 0x03
TARGET_FLASH_OP_VERIFY_CRC32 = 0x04
DEF_FIXTURE_GPIO = (0x0000, 0x0020, 0)
FIXTURE_GPIO_OP_CONFIGURE = 0x01
FIXTURE_GPIO_OP_READ_BANK = 0x02
DEF_FIXTURE_UART = (0x0000, 0x0021, 0)
FIXTURE_UART_OP_CONFIGURE = 0x01
FIXTURE_UART_OP_WRITE = 0x02
FIXTURE_UART_OP_READ = 0x03
DEF_FIXTURE_CAPTURE = (0x0000, 0x0022, 0)
FIXTURE_CAPTURE_OP_CONFIGURE = 0x01
FIXTURE_CAPTURE_OP_ARM = 0x02
FIXTURE_CAPTURE_OP_STATUS = 0x03
FIXTURE_CAPTURE_OP_READ = 0x04
DEF_P4_I2C_TARGET = (0x0100, 0x0001, 0)
P4_I2C_TARGET_OP_CONFIGURE = 0x01
P4_I2C_TARGET_OP_ARM_RX = 0x02
P4_I2C_TARGET_OP_READ_RX = 0x03
P4_I2C_TARGET_OP_PRELOAD_TX = 0x04
P4_I2C_TARGET_OP_STATUS = 0x05
P4_I2C_TARGET_OP_RESET = 0x06
P4_I2C_TARGET_OP_READ_HW = 0x10

@dataclass
class OfferedFunction:
    function: int = 0
    owner: int = 0
    id: int = 0
    revision: int = 0
    flags: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.function)
        out += struct.pack('<H', self.owner)
        out += struct.pack('<H', self.id)
        out += struct.pack('B', self.revision)
        out += struct.pack('B', self.flags)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'OfferedFunction':
        n = 0
        if len(data) != 8: raise ValueError('payload length must be 8')
        function = struct.unpack_from('<H', data, n)[0]; n += 2
        owner = struct.unpack_from('<H', data, n)[0]; n += 2
        id = struct.unpack_from('<H', data, n)[0]; n += 2
        revision = struct.unpack_from('B', data, n)[0]; n += 1
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(function=function, owner=owner, id=id, revision=revision, flags=flags)


@dataclass
class RequestHeader:
    ROLE = 0x01
    correlation: int = 0
    function: int = 0
    operation: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.correlation)
        out += struct.pack('<H', self.function)
        out += struct.pack('B', self.operation)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'RequestHeader':
        if len(data) < 6 or data[0] != cls.ROLE: raise ValueError('not a request header')
        data = data[1:6]
        n = 0
        if len(data) != 5: raise ValueError('payload length must be 5')
        correlation = struct.unpack_from('<H', data, n)[0]; n += 2
        function = struct.unpack_from('<H', data, n)[0]; n += 2
        operation = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(correlation=correlation, function=function, operation=operation)

    HEADER_LENGTH = 6


@dataclass
class ResultHeader:
    ROLE = 0x02
    correlation: int = 0
    resolution: int = 0
    detail: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.correlation)
        out += struct.pack('B', self.resolution)
        out += struct.pack('B', self.detail)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'ResultHeader':
        if len(data) < 5 or data[0] != cls.ROLE: raise ValueError('not a result header')
        data = data[1:5]
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        correlation = struct.unpack_from('<H', data, n)[0]; n += 2
        resolution = struct.unpack_from('B', data, n)[0]; n += 1
        detail = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(correlation=correlation, resolution=resolution, detail=detail)

    HEADER_LENGTH = 5


@dataclass
class ActivityUpdateHeader:
    ROLE = 0x03
    activity: int = 0
    kind: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.activity)
        out += struct.pack('B', self.kind)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'ActivityUpdateHeader':
        if len(data) < 4 or data[0] != cls.ROLE: raise ValueError('not a activity_update header')
        data = data[1:4]
        n = 0
        if len(data) != 3: raise ValueError('payload length must be 3')
        activity = struct.unpack_from('<H', data, n)[0]; n += 2
        kind = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(activity=activity, kind=kind)

    HEADER_LENGTH = 4


@dataclass
class ActivityOutcomeHeader:
    ROLE = 0x04
    activity: int = 0
    outcome: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.activity)
        out += struct.pack('B', self.outcome)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'ActivityOutcomeHeader':
        if len(data) < 4 or data[0] != cls.ROLE: raise ValueError('not a activity_outcome header')
        data = data[1:4]
        n = 0
        if len(data) != 3: raise ValueError('payload length must be 3')
        activity = struct.unpack_from('<H', data, n)[0]; n += 2
        outcome = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(activity=activity, outcome=outcome)

    HEADER_LENGTH = 4


@dataclass
class NotificationHeader:
    ROLE = 0x05
    function: int = 0
    kind: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.function)
        out += struct.pack('B', self.kind)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'NotificationHeader':
        if len(data) < 4 or data[0] != cls.ROLE: raise ValueError('not a notification header')
        data = data[1:4]
        n = 0
        if len(data) != 3: raise ValueError('payload length must be 3')
        function = struct.unpack_from('<H', data, n)[0]; n += 2
        kind = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(function=function, kind=kind)

    HEADER_LENGTH = 4


@dataclass
class DataHeader:
    ROLE = 0x06
    activity: int = 0
    kind: int = 0
    sequence: int = 0

    def pack(self) -> bytes:
        out = bytearray([self.ROLE])
        out += struct.pack('<H', self.activity)
        out += struct.pack('B', self.kind)
        out += struct.pack('<H', self.sequence)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'DataHeader':
        if len(data) < 6 or data[0] != cls.ROLE: raise ValueError('not a data header')
        data = data[1:6]
        n = 0
        if len(data) != 5: raise ValueError('payload length must be 5')
        activity = struct.unpack_from('<H', data, n)[0]; n += 2
        kind = struct.unpack_from('B', data, n)[0]; n += 1
        sequence = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(activity=activity, kind=kind, sequence=sequence)

    HEADER_LENGTH = 6


@dataclass
class CoreConfirmRequest:
    magic: bytes = b''
    min_revision: int = 0
    max_revision: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        if len(self.magic) != 4: raise ValueError('magic must be 4 bytes')
        out += bytes(self.magic)
        out += struct.pack('B', self.min_revision)
        out += struct.pack('B', self.max_revision)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreConfirmRequest':
        n = 0
        if len(data) != 6: raise ValueError('payload length must be 6')
        magic = bytes(data[n:n + 4]); n += 4
        min_revision = struct.unpack_from('B', data, n)[0]; n += 1
        max_revision = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(magic=magic, min_revision=min_revision, max_revision=max_revision)


@dataclass
class CoreConfirmResult:
    magic: bytes = b''
    revision: int = 0
    max_frame: int = 0
    window_bytes: int = 0
    max_inflight: int = 0
    flags: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        if len(self.magic) != 4: raise ValueError('magic must be 4 bytes')
        out += bytes(self.magic)
        out += struct.pack('B', self.revision)
        out += struct.pack('<H', self.max_frame)
        out += struct.pack('<H', self.window_bytes)
        out += struct.pack('B', self.max_inflight)
        out += struct.pack('B', self.flags)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreConfirmResult':
        n = 0
        if len(data) != 11: raise ValueError('payload length must be 11')
        magic = bytes(data[n:n + 4]); n += 4
        revision = struct.unpack_from('B', data, n)[0]; n += 1
        max_frame = struct.unpack_from('<H', data, n)[0]; n += 2
        window_bytes = struct.unpack_from('<H', data, n)[0]; n += 2
        max_inflight = struct.unpack_from('B', data, n)[0]; n += 1
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(magic=magic, revision=revision, max_frame=max_frame, window_bytes=window_bytes, max_inflight=max_inflight, flags=flags)


@dataclass
class CoreListRequest:
    first: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.first)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreListRequest':
        n = 0
        if len(data) != 1: raise ValueError('payload length must be 1')
        first = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(first=first)


@dataclass
class CoreListResult:
    total: int = 0
    entries: list[OfferedFunction] = field(default_factory=list)

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.total)
        if len(self.entries) > 16: raise ValueError('entries exceeds 16')
        out.append(len(self.entries))
        for item in self.entries: out += item.pack()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreListResult':
        n = 0
        if len(data) < 1: raise ValueError('short payload')
        total = struct.unpack_from('B', data, n)[0]; n += 1
        count = data[n]; n += 1
        if count > 16 or len(data) - n != count * 8: raise ValueError('bad array')
        entries = [OfferedFunction.unpack(data[n + i * 8:n + (i + 1) * 8]) for i in range(count)]; n += count * 8
        return cls(total=total, entries=entries)


@dataclass
class CoreDescribeRequest:
    function: int = 0
    first: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.function)
        out += struct.pack('B', self.first)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreDescribeRequest':
        n = 0
        if len(data) != 3: raise ValueError('payload length must be 3')
        function = struct.unpack_from('<H', data, n)[0]; n += 2
        first = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(function=function, first=first)


@dataclass
class CoreDescribeResult:
    tlv: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.tlv)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreDescribeResult':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        tlv = bytes(data[n:]); n = len(data)
        return cls(tlv=tlv)


@dataclass
class CorePlanApplyRequest:
    tlv: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.tlv)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePlanApplyRequest':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        tlv = bytes(data[n:]); n = len(data)
        return cls(tlv=tlv)


@dataclass
class CorePlanApplyResult:
    lease: int = 0
    tlv: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.lease)
        out += bytes(self.tlv)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePlanApplyResult':
        n = 0
        if len(data) < 2: raise ValueError('short payload')
        lease = struct.unpack_from('<H', data, n)[0]; n += 2
        tlv = bytes(data[n:]); n = len(data)
        return cls(lease=lease, tlv=tlv)


@dataclass
class CorePlanReleaseRequest:
    lease: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.lease)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePlanReleaseRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        lease = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(lease=lease)


@dataclass
class CorePlanReleaseResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePlanReleaseResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class CoreStopRequest:
    activity: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.activity)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreStopRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        activity = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(activity=activity)


@dataclass
class CoreStopResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CoreStopResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class CorePingRequest:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePingRequest':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class CorePingResult:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'CorePingResult':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class ProbeIdentityGetRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'ProbeIdentityGetRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class ProbeIdentityGetResult:
    profile_id: int = 0
    firmware_revision: int = 0
    reserved_pin_mask: int = 0
    fixture_pin_mask: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.profile_id)
        out += struct.pack('<I', self.firmware_revision)
        out += struct.pack('<Q', self.reserved_pin_mask)
        out += struct.pack('<Q', self.fixture_pin_mask)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'ProbeIdentityGetResult':
        n = 0
        if len(data) != 24: raise ValueError('payload length must be 24')
        profile_id = struct.unpack_from('<I', data, n)[0]; n += 4
        firmware_revision = struct.unpack_from('<I', data, n)[0]; n += 4
        reserved_pin_mask = struct.unpack_from('<Q', data, n)[0]; n += 8
        fixture_pin_mask = struct.unpack_from('<Q', data, n)[0]; n += 8
        return cls(profile_id=profile_id, firmware_revision=firmware_revision, reserved_pin_mask=reserved_pin_mask, fixture_pin_mask=fixture_pin_mask)


@dataclass
class TargetControlStatusRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlStatusRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetControlStatusResult:
    flags: int = 0
    start_mode: int = 0
    boot_status: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.flags)
        out += struct.pack('B', self.start_mode)
        out += struct.pack('B', self.boot_status)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlStatusResult':
        n = 0
        if len(data) != 3: raise ValueError('payload length must be 3')
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        start_mode = struct.unpack_from('B', data, n)[0]; n += 1
        boot_status = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(flags=flags, start_mode=start_mode, boot_status=boot_status)


@dataclass
class TargetControlResetRequest:
    mode: int = 0
    confirm: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.mode)
        out += struct.pack('B', self.confirm)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlResetRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        mode = struct.unpack_from('B', data, n)[0]; n += 1
        confirm = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(mode=mode, confirm=confirm)


@dataclass
class TargetControlResetResult:
    flags: int = 0
    attempts: int = 0
    pc: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.flags)
        out += struct.pack('B', self.attempts)
        out += struct.pack('<I', self.pc)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlResetResult':
        n = 0
        if len(data) != 6: raise ValueError('payload length must be 6')
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        attempts = struct.unpack_from('B', data, n)[0]; n += 1
        pc = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(flags=flags, attempts=attempts, pc=pc)


@dataclass
class TargetControlHaltRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlHaltRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetControlHaltResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlHaltResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetControlResumeRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlResumeRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetControlResumeResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlResumeResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetControlReadDmiRequest:
    address: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.address)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlReadDmiRequest':
        n = 0
        if len(data) != 1: raise ValueError('payload length must be 1')
        address = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(address=address)


@dataclass
class TargetControlReadDmiResult:
    value: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.value)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlReadDmiResult':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        value = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(value=value)


@dataclass
class TargetControlReadRegisterRequest:
    regno: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.regno)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlReadRegisterRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        regno = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(regno=regno)


@dataclass
class TargetControlReadRegisterResult:
    value: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.value)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetControlReadRegisterResult':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        value = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(value=value)


@dataclass
class TargetMemoryReadRequest:
    address: int = 0
    length: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.address)
        out += struct.pack('<H', self.length)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetMemoryReadRequest':
        n = 0
        if len(data) != 6: raise ValueError('payload length must be 6')
        address = struct.unpack_from('<I', data, n)[0]; n += 4
        length = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(address=address, length=length)


@dataclass
class TargetMemoryReadResult:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetMemoryReadResult':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class TargetMemoryWriteRequest:
    address: int = 0
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.address)
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetMemoryWriteRequest':
        n = 0
        if len(data) < 4: raise ValueError('short payload')
        address = struct.unpack_from('<I', data, n)[0]; n += 4
        data = bytes(data[n:]); n = len(data)
        return cls(address=address, data=data)


@dataclass
class TargetMemoryWriteResult:
    written: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.written)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetMemoryWriteResult':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        written = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(written=written)


@dataclass
class TargetFlashGeometryRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashGeometryRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetFlashGeometryResult:
    base: int = 0
    size: int = 0
    page: int = 0
    program_unit: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.base)
        out += struct.pack('<I', self.size)
        out += struct.pack('<H', self.page)
        out += struct.pack('<H', self.program_unit)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashGeometryResult':
        n = 0
        if len(data) != 12: raise ValueError('payload length must be 12')
        base = struct.unpack_from('<I', data, n)[0]; n += 4
        size = struct.unpack_from('<I', data, n)[0]; n += 4
        page = struct.unpack_from('<H', data, n)[0]; n += 2
        program_unit = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(base=base, size=size, page=page, program_unit=program_unit)


@dataclass
class TargetFlashErasePageRequest:
    address: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.address)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashErasePageRequest':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        address = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(address=address)


@dataclass
class TargetFlashErasePageResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashErasePageResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetFlashProgramPageRequest:
    address: int = 0
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.address)
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashProgramPageRequest':
        n = 0
        if len(data) < 4: raise ValueError('short payload')
        address = struct.unpack_from('<I', data, n)[0]; n += 4
        data = bytes(data[n:]); n = len(data)
        return cls(address=address, data=data)


@dataclass
class TargetFlashProgramPageResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashProgramPageResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class TargetFlashVerifyCrc32Request:
    address: int = 0
    length: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.address)
        out += struct.pack('<I', self.length)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashVerifyCrc32Request':
        n = 0
        if len(data) != 8: raise ValueError('payload length must be 8')
        address = struct.unpack_from('<I', data, n)[0]; n += 4
        length = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(address=address, length=length)


@dataclass
class TargetFlashVerifyCrc32Result:
    crc32: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.crc32)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'TargetFlashVerifyCrc32Result':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        crc32 = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(crc32=crc32)


@dataclass
class FixtureGpioConfigureRequest:
    channel: int = 0
    mode: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.channel)
        out += struct.pack('B', self.mode)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureGpioConfigureRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        channel = struct.unpack_from('B', data, n)[0]; n += 1
        mode = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(channel=channel, mode=mode)


@dataclass
class FixtureGpioConfigureResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureGpioConfigureResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class FixtureGpioReadBankRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureGpioReadBankRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class FixtureGpioReadBankResult:
    available: int = 0
    values: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<Q', self.available)
        out += struct.pack('<Q', self.values)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureGpioReadBankResult':
        n = 0
        if len(data) != 16: raise ValueError('payload length must be 16')
        available = struct.unpack_from('<Q', data, n)[0]; n += 8
        values = struct.unpack_from('<Q', data, n)[0]; n += 8
        return cls(available=available, values=values)


@dataclass
class FixtureUartConfigureRequest:
    baud: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.baud)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartConfigureRequest':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        baud = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(baud=baud)


@dataclass
class FixtureUartConfigureResult:
    actual_baud: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.actual_baud)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartConfigureResult':
        n = 0
        if len(data) != 4: raise ValueError('payload length must be 4')
        actual_baud = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(actual_baud=actual_baud)


@dataclass
class FixtureUartWriteRequest:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartWriteRequest':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class FixtureUartWriteResult:
    written: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.written)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartWriteResult':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        written = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(written=written)


@dataclass
class FixtureUartReadRequest:
    maximum: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.maximum)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartReadRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        maximum = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(maximum=maximum)


@dataclass
class FixtureUartReadResult:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureUartReadResult':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class FixtureCaptureConfigureRequest:
    sample_rate_hz: int = 0
    samples: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.sample_rate_hz)
        out += struct.pack('<I', self.samples)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureConfigureRequest':
        n = 0
        if len(data) != 8: raise ValueError('payload length must be 8')
        sample_rate_hz = struct.unpack_from('<I', data, n)[0]; n += 4
        samples = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(sample_rate_hz=sample_rate_hz, samples=samples)


@dataclass
class FixtureCaptureConfigureResult:
    actual_sample_rate_hz: int = 0
    samples: int = 0
    lines: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.actual_sample_rate_hz)
        out += struct.pack('<I', self.samples)
        out += struct.pack('B', self.lines)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureConfigureResult':
        n = 0
        if len(data) != 9: raise ValueError('payload length must be 9')
        actual_sample_rate_hz = struct.unpack_from('<I', data, n)[0]; n += 4
        samples = struct.unpack_from('<I', data, n)[0]; n += 4
        lines = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(actual_sample_rate_hz=actual_sample_rate_hz, samples=samples, lines=lines)


@dataclass
class FixtureCaptureArmRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureArmRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class FixtureCaptureArmResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureArmResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class FixtureCaptureStatusRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureStatusRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class FixtureCaptureStatusResult:
    flags: int = 0
    samples: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.flags)
        out += struct.pack('<I', self.samples)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureStatusResult':
        n = 0
        if len(data) != 5: raise ValueError('payload length must be 5')
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        samples = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(flags=flags, samples=samples)


@dataclass
class FixtureCaptureReadRequest:
    offset: int = 0
    maximum: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.offset)
        out += struct.pack('<H', self.maximum)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureReadRequest':
        n = 0
        if len(data) != 6: raise ValueError('payload length must be 6')
        offset = struct.unpack_from('<I', data, n)[0]; n += 4
        maximum = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(offset=offset, maximum=maximum)


@dataclass
class FixtureCaptureReadResult:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'FixtureCaptureReadResult':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class P4I2CTargetConfigureRequest:
    address: int = 0
    mode: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.address)
        out += struct.pack('B', self.mode)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetConfigureRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        address = struct.unpack_from('B', data, n)[0]; n += 1
        mode = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(address=address, mode=mode)


@dataclass
class P4I2CTargetConfigureResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetConfigureResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetArmRxRequest:
    length: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<H', self.length)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetArmRxRequest':
        n = 0
        if len(data) != 2: raise ValueError('payload length must be 2')
        length = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(length=length)


@dataclass
class P4I2CTargetArmRxResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetArmRxResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetReadRxRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetReadRxRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetReadRxResult:
    pending: int = 0
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.pending)
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetReadRxResult':
        n = 0
        if len(data) < 1: raise ValueError('short payload')
        pending = struct.unpack_from('B', data, n)[0]; n += 1
        data = bytes(data[n:]); n = len(data)
        return cls(pending=pending, data=data)


@dataclass
class P4I2CTargetPreloadTxRequest:
    data: bytes = b''

    def pack(self) -> bytes:
        out = bytearray()
        out += bytes(self.data)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetPreloadTxRequest':
        n = 0
        if len(data) < 0: raise ValueError('short payload')
        data = bytes(data[n:]); n = len(data)
        return cls(data=data)


@dataclass
class P4I2CTargetPreloadTxResult:
    slots: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.slots)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetPreloadTxResult':
        n = 0
        if len(data) != 1: raise ValueError('payload length must be 1')
        slots = struct.unpack_from('B', data, n)[0]; n += 1
        return cls(slots=slots)


@dataclass
class P4I2CTargetStatusRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetStatusRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetStatusResult:
    flags: int = 0
    rx_frames: int = 0
    tx_slots: int = 0
    errors: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('B', self.flags)
        out += struct.pack('<I', self.rx_frames)
        out += struct.pack('B', self.tx_slots)
        out += struct.pack('<H', self.errors)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetStatusResult':
        n = 0
        if len(data) != 8: raise ValueError('payload length must be 8')
        flags = struct.unpack_from('B', data, n)[0]; n += 1
        rx_frames = struct.unpack_from('<I', data, n)[0]; n += 4
        tx_slots = struct.unpack_from('B', data, n)[0]; n += 1
        errors = struct.unpack_from('<H', data, n)[0]; n += 2
        return cls(flags=flags, rx_frames=rx_frames, tx_slots=tx_slots, errors=errors)


@dataclass
class P4I2CTargetResetRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetResetRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetResetResult:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetResetResult':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetReadHwRequest:
    pass

    def pack(self) -> bytes:
        out = bytearray()
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetReadHwRequest':
        n = 0
        if len(data) != 0: raise ValueError('payload length must be 0')
        return cls()


@dataclass
class P4I2CTargetReadHwResult:
    sr: int = 0
    int_raw: int = 0
    fifo_st: int = 0
    ctr: int = 0
    slave_addr: int = 0
    filter_cfg: int = 0
    scl_stretch_conf: int = 0

    def pack(self) -> bytes:
        out = bytearray()
        out += struct.pack('<I', self.sr)
        out += struct.pack('<I', self.int_raw)
        out += struct.pack('<I', self.fifo_st)
        out += struct.pack('<I', self.ctr)
        out += struct.pack('<I', self.slave_addr)
        out += struct.pack('<I', self.filter_cfg)
        out += struct.pack('<I', self.scl_stretch_conf)
        return bytes(out)

    @classmethod
    def unpack(cls, data: bytes) -> 'P4I2CTargetReadHwResult':
        n = 0
        if len(data) != 28: raise ValueError('payload length must be 28')
        sr = struct.unpack_from('<I', data, n)[0]; n += 4
        int_raw = struct.unpack_from('<I', data, n)[0]; n += 4
        fifo_st = struct.unpack_from('<I', data, n)[0]; n += 4
        ctr = struct.unpack_from('<I', data, n)[0]; n += 4
        slave_addr = struct.unpack_from('<I', data, n)[0]; n += 4
        filter_cfg = struct.unpack_from('<I', data, n)[0]; n += 4
        scl_stretch_conf = struct.unpack_from('<I', data, n)[0]; n += 4
        return cls(sr=sr, int_raw=int_raw, fifo_st=fifo_st, ctr=ctr, slave_addr=slave_addr, filter_cfg=filter_cfg, scl_stretch_conf=scl_stretch_conf)


HEADER_CLASSES = {'request': RequestHeader, 'result': ResultHeader, 'activity_update': ActivityUpdateHeader, 'activity_outcome': ActivityOutcomeHeader, 'notification': NotificationHeader, 'data': DataHeader}
PAYLOAD_CLASSES = {
    ('core', 'confirm', 'request'): CoreConfirmRequest,
    ('core', 'confirm', 'result'): CoreConfirmResult,
    ('core', 'list', 'request'): CoreListRequest,
    ('core', 'list', 'result'): CoreListResult,
    ('core', 'describe', 'request'): CoreDescribeRequest,
    ('core', 'describe', 'result'): CoreDescribeResult,
    ('core', 'plan_apply', 'request'): CorePlanApplyRequest,
    ('core', 'plan_apply', 'result'): CorePlanApplyResult,
    ('core', 'plan_release', 'request'): CorePlanReleaseRequest,
    ('core', 'plan_release', 'result'): CorePlanReleaseResult,
    ('core', 'stop', 'request'): CoreStopRequest,
    ('core', 'stop', 'result'): CoreStopResult,
    ('core', 'ping', 'request'): CorePingRequest,
    ('core', 'ping', 'result'): CorePingResult,
    ('probe_identity', 'get', 'request'): ProbeIdentityGetRequest,
    ('probe_identity', 'get', 'result'): ProbeIdentityGetResult,
    ('target_control', 'status', 'request'): TargetControlStatusRequest,
    ('target_control', 'status', 'result'): TargetControlStatusResult,
    ('target_control', 'reset', 'request'): TargetControlResetRequest,
    ('target_control', 'reset', 'result'): TargetControlResetResult,
    ('target_control', 'halt', 'request'): TargetControlHaltRequest,
    ('target_control', 'halt', 'result'): TargetControlHaltResult,
    ('target_control', 'resume', 'request'): TargetControlResumeRequest,
    ('target_control', 'resume', 'result'): TargetControlResumeResult,
    ('target_control', 'read_dmi', 'request'): TargetControlReadDmiRequest,
    ('target_control', 'read_dmi', 'result'): TargetControlReadDmiResult,
    ('target_control', 'read_register', 'request'): TargetControlReadRegisterRequest,
    ('target_control', 'read_register', 'result'): TargetControlReadRegisterResult,
    ('target_memory', 'read', 'request'): TargetMemoryReadRequest,
    ('target_memory', 'read', 'result'): TargetMemoryReadResult,
    ('target_memory', 'write', 'request'): TargetMemoryWriteRequest,
    ('target_memory', 'write', 'result'): TargetMemoryWriteResult,
    ('target_flash', 'geometry', 'request'): TargetFlashGeometryRequest,
    ('target_flash', 'geometry', 'result'): TargetFlashGeometryResult,
    ('target_flash', 'erase_page', 'request'): TargetFlashErasePageRequest,
    ('target_flash', 'erase_page', 'result'): TargetFlashErasePageResult,
    ('target_flash', 'program_page', 'request'): TargetFlashProgramPageRequest,
    ('target_flash', 'program_page', 'result'): TargetFlashProgramPageResult,
    ('target_flash', 'verify_crc32', 'request'): TargetFlashVerifyCrc32Request,
    ('target_flash', 'verify_crc32', 'result'): TargetFlashVerifyCrc32Result,
    ('fixture_gpio', 'configure', 'request'): FixtureGpioConfigureRequest,
    ('fixture_gpio', 'configure', 'result'): FixtureGpioConfigureResult,
    ('fixture_gpio', 'read_bank', 'request'): FixtureGpioReadBankRequest,
    ('fixture_gpio', 'read_bank', 'result'): FixtureGpioReadBankResult,
    ('fixture_uart', 'configure', 'request'): FixtureUartConfigureRequest,
    ('fixture_uart', 'configure', 'result'): FixtureUartConfigureResult,
    ('fixture_uart', 'write', 'request'): FixtureUartWriteRequest,
    ('fixture_uart', 'write', 'result'): FixtureUartWriteResult,
    ('fixture_uart', 'read', 'request'): FixtureUartReadRequest,
    ('fixture_uart', 'read', 'result'): FixtureUartReadResult,
    ('fixture_capture', 'configure', 'request'): FixtureCaptureConfigureRequest,
    ('fixture_capture', 'configure', 'result'): FixtureCaptureConfigureResult,
    ('fixture_capture', 'arm', 'request'): FixtureCaptureArmRequest,
    ('fixture_capture', 'arm', 'result'): FixtureCaptureArmResult,
    ('fixture_capture', 'status', 'request'): FixtureCaptureStatusRequest,
    ('fixture_capture', 'status', 'result'): FixtureCaptureStatusResult,
    ('fixture_capture', 'read', 'request'): FixtureCaptureReadRequest,
    ('fixture_capture', 'read', 'result'): FixtureCaptureReadResult,
    ('p4_i2c_target', 'configure', 'request'): P4I2CTargetConfigureRequest,
    ('p4_i2c_target', 'configure', 'result'): P4I2CTargetConfigureResult,
    ('p4_i2c_target', 'arm_rx', 'request'): P4I2CTargetArmRxRequest,
    ('p4_i2c_target', 'arm_rx', 'result'): P4I2CTargetArmRxResult,
    ('p4_i2c_target', 'read_rx', 'request'): P4I2CTargetReadRxRequest,
    ('p4_i2c_target', 'read_rx', 'result'): P4I2CTargetReadRxResult,
    ('p4_i2c_target', 'preload_tx', 'request'): P4I2CTargetPreloadTxRequest,
    ('p4_i2c_target', 'preload_tx', 'result'): P4I2CTargetPreloadTxResult,
    ('p4_i2c_target', 'status', 'request'): P4I2CTargetStatusRequest,
    ('p4_i2c_target', 'status', 'result'): P4I2CTargetStatusResult,
    ('p4_i2c_target', 'reset', 'request'): P4I2CTargetResetRequest,
    ('p4_i2c_target', 'reset', 'result'): P4I2CTargetResetResult,
    ('p4_i2c_target', 'read_hw', 'request'): P4I2CTargetReadHwRequest,
    ('p4_i2c_target', 'read_hw', 'result'): P4I2CTargetReadHwResult,
}

def message_role(msg: bytes) -> int:
    """Role byte of a message, or 0 when empty or unknown."""
    return msg[0] if msg and msg[0] in ROLES.values() else 0

